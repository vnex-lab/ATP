name = "example_mod"
description = "Example mod module for applying project changes or custom runtime behavior."


def apply_mod():
    message = "Example mod applied: this module can modify code, config, or assets."
    print(message)
    return message
